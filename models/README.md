# 本地模型目录

本目录用于放置 FunASR 等本地模型权重。实际权重不提交 Git，也不保存到 SQLite。

当前配置应至少指定：

- ASR 模型；
- VAD 模型；
- 标点模型；
- 可选说话人模型。

Worker 启动时会：

1. 读取 manifest；
2. 检查目录和必要文件；
3. 加载模型；
4. 运行标准短音频；
5. 记录模型身份、版本、配置哈希和健康结果。

目录中存在的其他模型不会自动进入产品链路。只有完成配置和测试后才注册。
